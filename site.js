const hero=document.querySelector('.hero'),after=document.querySelector('.hero-after');if(hero&&after&&matchMedia('(pointer:fine)').matches){hero.addEventListener('pointermove',e=>{const r=hero.getBoundingClientRect();after.style.setProperty('--mx',((e.clientX-r.left)/r.width*100)+'%');after.style.setProperty('--my',((e.clientY-r.top)/r.height*100)+'%')})}document.querySelectorAll('.compare').forEach(el=>{let drag=false;const set=x=>{const r=el.getBoundingClientRect(),p=Math.max(2,Math.min(98,(x-r.left)/r.width*100));el.style.setProperty('--pos',p+'%')};el.addEventListener('pointerdown',e=>{drag=true;el.setPointerCapture(e.pointerId);set(e.clientX)});el.addEventListener('pointermove',e=>drag&&set(e.clientX));el.addEventListener('pointerup',()=>drag=false);el.addEventListener('pointercancel',()=>drag=false)});